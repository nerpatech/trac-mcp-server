# Own config so this suite does not inherit the MCP server's addopts
# (--ruff --ruff-format), whose plugins are not installed in this venv.
# This tree is a Trac plugin for the Trac host, not part of that package.
[pytest]
testpaths = tests
filterwarnings =
    ignore:pkg_resources is deprecated:UserWarning
